services:
  callback:
    build: .
    ports:
      - "3001:3000"
    depends_on:
      - kafka
    environment:
      - KAFKA_BROKERS=kafka:9093
      - HMAC_SECRET=${HMAC_SECRET:-super-secret-change-in-prod}
    restart: unless-stopped
